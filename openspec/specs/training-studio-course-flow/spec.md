# training-studio-course-flow Specification

## Purpose

TBD - created by archiving change course-order-and-mandala. Update Purpose after archive.
## Requirements
### Requirement: シリーズ内のコース順序は配列順である

シリーズ内のコース間の受講順序は、`series.courses` の並びのみで定義されなければならない（SHALL）。Pane1 のドラッグ＆ドロップによる並べ替えは、その配列を更新し、シリーズ内の受講順も更新しなければならない（SHALL）。

#### Scenario: 並べ替えで受講順が更新される

- **WHEN** ユーザーが Pane1 でシリーズ内のコースを並べ替える
- **THEN** `series.courses` の配列順が新しい一覧順と一致する
- **AND** そのシリーズのシリーズ内受講順が新しい順序と一致する

#### Scenario: 先頭コースにシリーズ内の前コースはない

- **WHEN** コースが `series.courses` の先頭である
- **THEN** 配列順から導出されるシリーズ内の前コースは存在しない

### Requirement: 別シリーズのリンクはコースメタにのみ保存する

各コースの `cross_series_prev` および `cross_series_next` には、当該コースの親シリーズ以外に属するコース ID のみを保存しなければならない（SHALL）。エディタは、これらの配列にシリーズ内の隣接コース ID を永続化してはならない（MUST NOT）。

#### Scenario: 保存時に同シリーズ ID を除外する

- **WHEN** ユーザーが前提・次コースを含むコースメタを保存する
- **THEN** 同一親シリーズ内のコースを指す ID は保存配列から除外される

#### Scenario: 読み込み時にレガシーな同シリーズ ID を正規化する

- **WHEN** ワークスペースデータを読み込み、コースメタ配列に当該コースと同じシリーズの ID が含まれる
- **THEN** UI で使う前にそれらの ID が除去される

#### Scenario: 並べ替えは別シリーズメタを変えない

- **WHEN** ユーザーがシリーズ内のコースのみを並べ替える
- **THEN** 全コースの `cross_series_prev` と `cross_series_next` は、読み込み時正規化を除き変更されない

### Requirement: コースメタ編集モーダルの項目

コースメタ編集ダイアログは、次の項目を提供しなければならない（SHALL）: 編集可能な受講対象者（単一行テキスト）; 編集可能な受講形態（select。未設定 / 独習 / 講義 / ハンズオンの4択、`style` に `self-study` / `lecture` / `hands-on` を保存し「未設定」ではキーを書かない）; 読取専用の同シリーズの前コース（最大1件、`i-1` の日本語表示名）; 読取専用の同シリーズの次コース（最大1件、`i+1` の日本語表示名）; 編集可能な別シリーズの前コース（0件以上、日本語名で多選択、`cross_series_prev` に ID 保存）; 編集可能な別シリーズの次コース（0件以上、多選択、`cross_series_next` に ID 保存）。

「前のコース（別シリーズ）」の候補リストの先頭には、シリーズ見出しを持たない特殊枠として **`Start` のチェックボックス**を表示しなければならない（SHALL）。同様に「次のコース（別シリーズ）」側には **`Goal` のチェックボックス**を表示する。チェック状態はそれぞれコース `.meta.json` の `is_start` / `is_goal` に保存し、`cross_series_prev` / `cross_series_next` の配列に混ぜてはならない（SHALL NOT）。Start / Goal のチェックは他の候補選択と独立でなければならない（SHALL）——「各シリーズ1件まで」のルールに関与せず、排他・無効化を行わない。

#### Scenario: Start を宣言して保存する

- **WHEN** メタダイアログで「前のコース（別シリーズ）」の特殊枠 `Start` をチェックして保存する
- **THEN** コース `.meta.json` に `"is_start": true` が書き込まれ、`cross_series_prev` は変化しない
- **AND** ダイアログを開き直すと `Start` がチェックされている

#### Scenario: Start と前のコースが共存できる

- **WHEN** 別シリーズの前コースを選択済みのコースで、さらに `Start` をチェックして保存する
- **THEN** 両方が保存され、エラー・警告・自動解除は発生しない

#### Scenario: 同シリーズの前コースを読取表示する

- **WHEN** シリーズ内で先頭以外のコースについてメタダイアログを開く
- **THEN** ダイアログに `series.courses[i-1]` の日本語名が同シリーズの前コースとして表示される
- **AND** その項目は編集できない

#### Scenario: シリーズ先頭では同シリーズの前コースなし

- **WHEN** シリーズ先頭のコースについてメタダイアログを開く
- **THEN** 同シリーズの前コース欄はなし（例: 「なし」）と表示される

#### Scenario: 別シリーズの選択は ID として保存する

- **WHEN** ユーザーがメタダイアログで別シリーズのコースを選択して保存する
- **THEN** `cross_series_prev` と `cross_series_next` には選択した別シリーズのコース ID のみが含まれる

#### Scenario: 受講形態を選んで保存する

- **WHEN** メタダイアログで受講形態に「ハンズオン」を選んで保存する
- **THEN** コース `.meta.json` の `style` に `hands-on` が書き込まれる
- **AND** ダイアログを開き直すと「ハンズオン」が選択されている

#### Scenario: 受講形態を未設定に戻す

- **WHEN** `style` が設定済みのコースで受講形態に「未設定」を選んで保存する
- **THEN** コース `.meta.json` から `style` キーが除去される

### Requirement: ミニ曼陀羅のローカルグラフ

コースが選択されているとき、統合ツリーペイン下部の固定領域は現在コースを中心としたミニ曼陀羅（Mermaid）を描画しなければならない（SHALL）。コースもレッスンも選択されていないときは領域を畳む。表示内容は次のとおり: シリーズ内の前ノードは最大1件; シリーズ内の次ノードは最大1件; `cross_series_prev` に列挙された別シリーズのコースはすべて; `cross_series_next` に列挙された別シリーズのコースはすべて。シリーズ内のエッジは、該当ノードがあるとき `前 → 現在 → 次` としなければならない（SHALL）。別シリーズのエッジは、リンクごとに `前提 → 現在` および `現在 → 次` としなければならない（SHALL）。

現在コースが `is_start` を宣言しているときは、枠を持たない文字ノード `Start` から現在コースへのエッジを表示しなければならない（SHALL）。`is_goal` を宣言しているときは、現在コースから文字ノード `Goal` へのエッジを表示しなければならない（SHALL）。宣言が無いコースでは Start / Goal ノードを表示しない。

ミニ曼陀羅とグローバル曼陀羅の接続情報取得は、共通のヘルパー関数 `buildCourseNeighbors` を通じて行わなければならない（SHALL）。各曼陀羅が独立して配列走査とリンク解決を行ってはならない（MUST NOT）。

#### Scenario: Start 宣言コースのミニグラフ

- **WHEN** `is_start: true` のコースを選択してミニ曼陀羅を表示する
- **THEN** 文字ノード `Start` から現在コースへのエッジが表示される
- **AND** 前のコースが存在する場合、そのエッジも同時に表示される

#### Scenario: ミニグラフにシリーズ内の前後を含む

- **WHEN** 選択コースがシリーズ内で index `i` にあり、`i > 0` かつ `i < length - 1` である
- **THEN** ミニ曼陀羅に `courses[i-1]` と `courses[i+1]` のノードが現在コースへつながって表示される

#### Scenario: ミニグラフに別シリーズリンクをすべて含む

- **WHEN** 選択コースのメタに別シリーズの前コースが2件（`cross_series_prev`）、次コースが1件（`cross_series_next`）ある
- **THEN** ミニ曼陀羅に前コース2ノードから現在へのエッジと、現在から次コース1ノードへのエッジが表示される

#### Scenario: ミニグラフのノードクリックでコース選択

- **WHEN** ユーザーがミニ曼陀羅（サムネイルまたは拡大モーダル）で現在以外のノードをクリックする
- **THEN** ワークスペースがそのコース ID を選択する

### Requirement: グローバル曼陀羅はコース順序を反映する

グローバル曼陀羅（`GlobalHeader` ダイアログ）は、各シリーズの隣接ペアごとに `courses[i-1]` から `courses[i]` へのエッジを描画しなければならない（SHALL）。別シリーズのコースに解決する `cross_series_prev` の各エントリについて `前提 → 当該` のエッジを、`cross_series_next` の各エントリについて `当該 → 次` のエッジを描画しなければならない（SHALL）。同一のコース対 `(from, to)` について、両配列から同じ向きのリンクが得られる場合はエッジを1本にまとめなければならない（SHALL）。`series` が変更されたとき（シリーズ内の並べ替えを含む）、次回描画で使う曼陀羅定義は、シリーズ内の鎖について更新後の配列順を反映しなければならない（MUST）。

`is_start` を宣言したコースごとに、シリーズのサブグラフ外の文字ノード `Start` から当該コースへのエッジを描画しなければならない（SHALL）。`is_goal` を宣言したコースごとに、当該コースから文字ノード `Goal` へのエッジを描画しなければならない（SHALL）。Start / Goal ノードは宣言したコースごとに個別に置き、複数の宣言を1つのノードに集約してはならない（SHALL NOT）。

#### Scenario: グローバル曼陀羅にシリーズ内の鎖がある

- **WHEN** シリーズのコースが配列順で `[A, B, C]` である
- **THEN** グローバル曼陀羅の当該サブグラフ内に `A → B` と `B → C` のエッジがある

#### Scenario: 並べ替え後にグローバル曼陀羅が更新される

- **WHEN** ユーザーがシリーズ内でコースを並べ替え、`series` 状態が更新されたうえでグローバル曼陀羅を開くまたは再描画する
- **THEN** シリーズ内エッジが新しい `courses[]` の順序と一致する

#### Scenario: 別シリーズの前提コースがグローバル曼陀羅に表示される

- **WHEN** コース X の `cross_series_prev` に別シリーズのコース P がある
- **THEN** グローバル曼陀羅に `P → X` のエッジがある

#### Scenario: 双方向メタでも別シリーズエッジは1本

- **WHEN** コース A の `cross_series_next` に B があり、かつコース B の `cross_series_prev` に A がある（いずれも別シリーズ）
- **THEN** グローバル曼陀羅に `A → B` のエッジは1本だけある

#### Scenario: 並べ替えでも別シリーズエッジは維持される

- **WHEN** コース X の `cross_series_next` に別シリーズのコース Y があり、ユーザーは X の属するシリーズ内のみ並べ替える
- **THEN** グローバル曼陀羅に `X → Y` のエッジが残る

#### Scenario: 複数の Start 宣言はそれぞれ描かれる

- **WHEN** 2つのコースが `is_start: true` を宣言している
- **THEN** グローバル曼陀羅に `Start` の文字ノードが2つ、それぞれの宣言コースへのエッジ付きで表示される

### Requirement: 別シリーズリンク保存時の双方向同期

コースメタ保存時、エディタは編集対象コースの別シリーズ `cross_series_prev` および `cross_series_next` を正規化したうえで、ワークスペース内の関連コースを更新し、次を満たさなければならない（SHALL）:

- **ミラー**: 別シリーズのコース `X` から `Y` へのリンクが存在するとき、`X.cross_series_next` に `Y` が含まれ、かつ `Y.cross_series_prev` に `X` が含まれる。
- **ソースシリーズ排他**: 任意のターゲットコース `Y` とソースシリーズ `S` について、`Y.cross_series_prev` に含まれる `S` 内のコースは高々1つである。
- **ターゲットシリーズ排他（出口）**: 任意の出口元コース `X` とターゲットシリーズ `T` について、`X.cross_series_next` に含まれる `T` 内のコースは高々1つである。`X → Y`（`Y` は `T` 内）を新たに確立するとき、同じ `T` 内の他コース `Z` への `X.cross_series_next` および `Z.cross_series_prev` 内の `X` は除去されなければならない（SHALL）。これにより、コースメタ picker の「各シリーズから1コースまで」と保存後の `cross_series_next` が一致する。
- **編集優先**: ユーザーがコース `E` のメタを保存し、`E` がシリーズ `S` からターゲット `Y` への別シリーズリンクを設定した場合、`S` 内で `Y` への唯一のゲートウェイは `E` となる。同シリーズ `S` の他コースに残っていた `Y` への `cross_series_next` および `Y.cross_series_prev` 内の `S` 由来の古い ID は除去されなければならない（SHALL）。
- **多シリーズ合流**: 上記排他はソースシリーズごとに適用される。異なるソースシリーズから同一ターゲット `Y` へのリンク（例: `A1 → Y` と `B1 → Y`）は同時に存在してよい（SHALL）。

リンク削除時、エディタは除去された向きのミラー側エントリも削除しなければならない（SHALL）。削除に伴い他コースへリンクを自動付け替えてはならない（MUST NOT）。

#### Scenario: 同シリーズ内の出口を付け替える

- **WHEN** シリーズ A に `A1`, `A2`、シリーズ B に `B1` があり、当初 `A2.cross_series_next` に `B1`、`B1.cross_series_prev` に `A2` がある
- **AND** ユーザーが `A1` のメタを保存し、別シリーズの次コースに `B1` を設定する
- **THEN** `A1.cross_series_next` に `B1` が含まれる
- **AND** `A2.cross_series_next` から `B1` が除去される
- **AND** `B1.cross_series_prev` はシリーズ A 由来として `A2` の代わりに `A1` のみを含む

#### Scenario: 異なるソースシリーズからの合流は維持する

- **WHEN** `A1.cross_series_next` に `B2`、`B1.cross_series_next` に `B2` があり、それぞれミラーが整合している
- **AND** ユーザーが `A2` のメタを保存し、別シリーズの次コースに `B2` を設定する
- **THEN** `A2.cross_series_next` に `B2` が含まれ、`A1.cross_series_next` から `B2` が除去される
- **AND** `B2.cross_series_prev` のシリーズ A 由来は `A2` のみとなる
- **AND** `B1.cross_series_next` および `B2.cross_series_prev` のシリーズ B 由来 `B1` は変更されない

#### Scenario: cross_series_prev 側の編集でもミラーと排他が成立する

- **WHEN** `A2.cross_series_next` に `B1`、`B1.cross_series_prev` に `A2` がある
- **AND** ユーザーが `B1` のメタを保存し、別シリーズの前コースに `A1` を設定する
- **THEN** `A1.cross_series_next` に `B1` が含まれ、`A2.cross_series_next` から `B1` が除去される
- **AND** `B1.cross_series_prev` のシリーズ A 由来は `A1` のみとなる

#### Scenario: 入口編集で同一ターゲットシリーズへの古い出口が除去される

- **WHEN** シリーズ A に `A1`, `A2`、シリーズ B に `B1`, `B2` があり、当初 `A1.cross_series_next` に `B1`、`A2.cross_series_next` に `B2` でミラーが整合している
- **AND** ユーザーが `B2` のメタを保存し、別シリーズの前コースを `A2` から `A1` に変更する
- **THEN** `A1.cross_series_next` に `B2` が含まれ、同シリーズ B 向けの `B1` は含まれない
- **AND** `A2.cross_series_next` から `B2` が除去される
- **AND** `B1.cross_series_prev` から `A1` が除去される
- **AND** `B2.cross_series_prev` のシリーズ A 由来は `A1` のみとなる

#### Scenario: リンク削除でミラーが除去される

- **WHEN** `A1.cross_series_next` に `B1`、`B1.cross_series_prev` に `A1` がある
- **AND** ユーザーが `A1` のメタを保存し、別シリーズの次コースから `B1` を外す
- **THEN** `A1.cross_series_next` に `B1` が含まれない
- **AND** `B1.cross_series_prev` から `A1` が除去される
- **AND** 他コースに `B1` への新しいリンクが自動追加されない

#### Scenario: UI の同一シリーズ1選択制約と保存結果が一致する

- **WHEN** コースメタ編集ダイアログで別シリーズの前/次コースを選択する
- **THEN** 各 picker 内では同一シリーズから高々1コースのみ選択可能である（既存 UI 動作）
- **AND** 保存後、任意のコース `X` の `cross_series_next` において、同一ターゲットシリーズからの ID は高々1つである
- **AND** 保存後のワークスペース状態は上記ミラー・排他・編集優先の Requirement を満たす

### Requirement: コースメタ保存前の曼陀羅循環チェック

コースメタ保存前、エディタは編集内容を `applyCrossSeriesCourseMetaEdit` と同等のドライランで反映したうえで、曼陀羅全体の有向グラフ（シリーズ内の `courses[]` 鎖および別シリーズの `cross_series_prev` / `cross_series_next` リンク）に循環が生じるかを評価しなければならない（SHALL）。グラフ構築および巡回判定は、グローバル曼陀羅描画と同型の辺集合を用いなければならない（SHALL）。

別シリーズリンクの変更により、ドライラン後のグラフに新たな循環が生じる場合、コースメタ編集ダイアログは保存前に警告を表示し、保存を完了してはならない（MUST NOT）。警告は別シリーズの前/次コース設定の見直しを促す内容としなければならない（SHALL）。別シリーズリンクを変更せず、当該コースの名称や受講対象者のみを保存する場合、既存データに循環があっても保存をブロックしてはならない（MUST NOT）。

#### Scenario: 循環を生む別シリーズリンク変更は保存できない

- **WHEN** ユーザーがコースメタ編集ダイアログで別シリーズの前/次コースを変更し、保存を試みる
- **AND** ドライラン後の曼陀羅全体に循環する経路が存在する
- **AND** 変更前のワークスペース状態には循環がなかった、または当該コースの別シリーズリンクが変更されている
- **THEN** ダイアログに循環に関する警告が表示される
- **AND** 保存は実行されず、ダイアログは開いたままである

#### Scenario: 非リンク項目のみの保存は循環があってもブロックしない

- **WHEN** ワークスペースの曼陀羅全体に既に循環がある
- **AND** ユーザーが当該コースの別シリーズ前/次を変更せず、コース名または受講対象者のみを保存する
- **THEN** 循環警告は表示されない
- **AND** 保存が完了する

#### Scenario: 循環のない変更は通常どおり保存できる

- **WHEN** ユーザーが別シリーズリンクを変更して保存を試みる
- **AND** ドライラン後の曼陀羅全体に循環がない
- **THEN** 警告は表示されない
- **AND** リンク同期 Requirement に従って保存が完了する

### Requirement: 別シリーズコース選択 UI はダークで十分なコントラストを満たす

コースメタ編集ダイアログ内の別シリーズ前/次コース選択 UI（`CrossSeriesCourseTreePicker`）は、ダークテーマ時にリスト背景・シリーズ見出し・選択行・ホバー行が識別可能なコントラストでなければならない（SHALL）。詳細は `training-studio-workspace-dark-mode` の曼陀羅・ピッカー要件と整合させなければならない（SHALL）。

#### Scenario: ダークでチェック済みコースが見える

- **WHEN** テーマが `dark` である
- **AND** ユーザーが別シリーズのコースを 1 件以上選択している
- **THEN** チェック済み行が未選択行と明確に区別できる

### Requirement: グローバル曼陀羅のノードクリックでコース選択（ダイアログは開いたまま）

グローバル曼陀羅（`GlobalHeader` ダイアログ）でコースノードをクリックしたとき、ワークスペースはそのコース ID を選択しなければならない（SHALL）。コース選択後もグローバル曼陀羅ダイアログは開いたままでなければならない（SHALL）。選択コースが変わったとき、開いたままの曼陀羅内で現在選択中コース（青枠・太字）の表示は更新されなければならない（SHALL）。ユーザーが × ボタン、Esc、オーバーレイクリック等で明示的に閉じた場合のみ、ダイアログは閉じなければならない（SHALL）。

#### Scenario: グローバル曼陀羅のノードクリックでコース選択しダイアログは開いたまま

- **WHEN** ユーザーがグローバル曼陀羅を開き、現在選択中以外のコースノードをクリックする
- **THEN** ワークスペースがそのコース ID を選択する
- **AND** グローバル曼陀羅ダイアログは開いたままである

#### Scenario: コース選択後に現在コースのハイライトが更新される

- **WHEN** ユーザーがグローバル曼陀羅を開いたまま別のコースノードをクリックし、選択コースが変わる
- **THEN** 曼陀羅内の青枠・太字（現在選択中コース）表示が新しい選択コースに移る

#### Scenario: 明示的な操作でのみダイアログを閉じる

- **WHEN** ユーザーがグローバル曼陀羅を開き、コースノードをクリックせず × ボタンまたは Esc でダイアログを閉じる
- **THEN** グローバル曼陀羅ダイアログが閉じる

