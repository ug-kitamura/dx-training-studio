# content-structure-sync Specification

## Purpose

UI 操作（追加・リネーム・並び替え・削除）と `contents/` ファイルシステムの同期、およびファイル名サニタイズの要件を規定する。
## Requirements
### Requirement: レッスン追加時のファイル生成
UI でレッスンを追加した場合、コースフォルダ内に適切な数値プレフィックスを持つ `.md` ファイルを生成しなければならない（SHALL）。ファイルにはフロントマターとデフォルト本文を含めなければならない（SHALL）。

#### Scenario: レッスンを末尾に追加する
- **WHEN** ユーザーが UI で既存コースにレッスンを追加し、レッスン名を入力する
- **THEN** `contents/<シリーズ>/<コース>/<N+1>_<レッスン名>.md` が生成され、フロントマターとデフォルト本文が書き込まれている

### Requirement: コース追加時のフォルダ生成
UI でコースを追加した場合、シリーズフォルダ内に適切な数値プレフィックスを持つコースフォルダと空の `_course.json` を生成しなければならない（SHALL）。

#### Scenario: コースを末尾に追加する
- **WHEN** ユーザーが UI でシリーズにコースを追加し、コース名を入力する
- **THEN** `contents/<シリーズ>/<N+1>_<コース名>/` フォルダと `_course.json` が生成されている

### Requirement: シリーズ追加時のフォルダ生成
UI でシリーズを追加した場合、`contents/` 直下にシリーズフォルダを生成し、`_series-order.json` を更新しなければならない（SHALL）。

#### Scenario: シリーズを追加する
- **WHEN** ユーザーが UI でシリーズを追加し、シリーズ名を入力する
- **THEN** `contents/<シリーズ名>/` フォルダが生成され、`_series-order.json` の末尾にシリーズ名が追加されている

### Requirement: リネーム時のフォルダ/ファイル名変更
UI でシリーズ・コース・レッスンの名前を変更した場合、対応するフォルダ名またはファイル名を変更しなければならない（SHALL）。数値プレフィックスは保持されなければならない（SHALL）。

#### Scenario: コースをリネームする
- **WHEN** ユーザーが UI でコース名を `旧コース名` から `新コース名` に変更する
- **THEN** `01_旧コース名/` フォルダが `01_新コース名/` にリネームされている

#### Scenario: レッスンをリネームする
- **WHEN** ユーザーが UI でレッスン名を `旧レッスン名` から `新レッスン名` に変更する
- **THEN** `02_旧レッスン名.md` が `02_新レッスン名.md` にリネームされ、フロントマターの `lesson:` フィールドも更新されている

### Requirement: 並び替え時の数値プレフィックス振り直し
UI でコースまたはレッスンの表示順を変更した場合、同一階層内のすべてのフォルダ/ファイルの数値プレフィックスを振り直さなければならない（SHALL）。

#### Scenario: レッスンを上位に移動する
- **WHEN** ユーザーが UI で `03_レッスンC` を 1番目に移動する
- **THEN** ファイル名が `01_レッスンC`, `02_旧01_レッスンA`, `03_旧02_レッスンB` にリネームされる

#### Scenario: 並び替え中にエラーが発生する
- **WHEN** リネーム操作中にサーバーエラーが発生する
- **THEN** ロールバックを試み、UI にエラートーストを表示する。state は変更前に戻す

### Requirement: 削除時のフォルダ/ファイル削除
UI でシリーズ・コース・レッスンを削除した場合、対応するフォルダまたはファイルを削除しなければならない（SHALL）。コース削除時は配下のすべてのレッスン `.md` ファイルも削除されなければならない（SHALL）。

#### Scenario: レッスンを削除する
- **WHEN** ユーザーが UI でレッスンを削除する
- **THEN** 対応する `.md` ファイルが削除され、同一コース内の残りレッスンの数値プレフィックスが振り直される

### Requirement: ファイルシステム禁止文字のサニタイズ

シリーズ・コース・レッスン名にファイルシステム禁止文字（`/`, `\`, `:`, `*`, `?`, `"`, `<`, `>`, `|`）が含まれる場合、`_` に置換してフォルダ名に使用しなければならない（SHALL）。この置換は最後の安全網であり、廃止してはならない（SHALL NOT）。

**置換後のフォルダ名がそのまま表示名になる**（SHALL）——`flat-path-names` の定めるとおり、フォルダ名は表示名そのものであり、名前の正本はフォルダ名である。したがって置換前の名前を表示名として保持することはできない（MUST NOT 期待）。置換が起きた名前は、受講者に見えるサイドバー・タブ・曼陀羅にも `_` を含んだまま現れる。

#### Scenario: 禁止文字を含む名前でレッスンを作成する

- **WHEN** ユーザーが `レッスン/テスト` というレッスン名を入力する
- **THEN** フォルダ名は `レッスン_テスト` となる
- **AND** そのレッスンの表示名も `レッスン_テスト` であり、`レッスン/テスト` はどこにも保持されない

### Requirement: 名前にファイル名として使えない文字を使わない

シリーズ・コース・レッスンの名前には、フォルダ名として安全でない文字を使ってはならない（SHALL NOT）。フォルダ名が表示名を兼ねる以上、置換されれば受講者に見える名前が変わるためである。避けなければならないものは次のとおり（SHALL）:

- ファイルシステム禁止文字: `/` `\` `:` `*` `?` `"` `<` `>` `|`
- 末尾のドットおよび前後の空白（Windows が作成時に落とすため、`order` の文字列と実体がずれる）
- 制御文字（U+0000–U+001F）
- Windows の予約デバイス名: `CON` `PRN` `AUX` `NUL` `COM1`–`COM9` `LPT1`–`LPT9`（拡張子を付けても同じ）
- 同一階層内で大文字小文字だけが異なる名前（Windows・macOS では衝突し、Linux では別物になるため CI でだけ壊れる）

区切りを表したいときは全角スラッシュ `／` または中黒 `・` を使う（SHALL）。

この規則は名前を決める工程（`training-plan-skill` / `training-create-skill`）が守るものであり、API が入力を拒否することを求めるものではない（SHALL NOT）——置換は安全網として残る。

#### Scenario: 区切りを表す名前

- **WHEN** レッスン名で「リポジトリを作る」と「clone」を並べたい
- **THEN** 名前は `リポジトリを作る／clone` であり `リポジトリを作る / clone` ではない

#### Scenario: 置換が起きても API は成功する

- **WHEN** 禁止文字を含む名前で作成 API を呼ぶ
- **THEN** リクエストは拒否されず、フォルダ名は `_` に置換された名前で作られる

### Requirement: 複製時のフォルダ複製と id・slug の扱い

複製 API はシリーズ / コース / レッスンのフォルダを受け入れ先へ再帰コピーしなければならない（SHALL）。複製後、コピーされた各エンティティのメタから `id` と `slug` を除去しなければならない（SHALL）——`id` はローダーの自動採番に委ね、`slug` は人が後で設定する。コピー先の名前は「元の名前（コピー）」とし、既存名と衝突する場合は「（コピー2）」以降の連番で回避しなければならない（SHALL）。本文および `id` / `slug` 以外のメタフィールドは元の値を保持しなければならない（SHALL）。

シリーズ・コースの複製では、配下の全子孫（コース・レッスン）についても同様に `id` / `slug` を除去しなければならない（SHALL）。複製されたエンティティは受け入れ先の並び順（`order`）の末尾に追加されなければならない（SHALL）。

#### Scenario: コースを複製すると配下レッスンの id も新規になる

- **WHEN** レッスン2本を持つコースを複製する
- **THEN** 複製されたコースと配下レッスン2本はいずれも元と異なる新しい `id` を持ち、`slug` は未設定である
- **AND** レッスン本文は元と同一である

#### Scenario: 複製は order の末尾に付く

- **WHEN** シリーズ内でコースを複製する
- **THEN** シリーズ `.meta.json` の `order` の末尾に複製コースが追加されている

#### Scenario: 元のエンティティは変化しない

- **WHEN** 任意のエンティティを複製する
- **THEN** 元のフォルダ・メタ・本文は複製前と同一である

